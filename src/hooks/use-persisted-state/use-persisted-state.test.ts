import React from 'react';

describe('usePersistedState', () => {
    it.todo('returns default value if no value is persisted')
    it.todo('returns the persisted value if valid')
    it.todo('persists the value in local storage when the value is updated')
})